{
  "name": "glass-login-ui",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "An accessible glassmorphism login UI demo with local validation and zero runtime dependencies.",
  "scripts": {
    "test": "node --test",
    "check:syntax": "node --check assets/js/login-form.js && node --check assets/js/storage.js && node --check assets/js/javas.js && node --check tests/login-form.test.js && node --check tests/storage.test.js && node --check tests/markup.test.js",
    "check": "npm run check:syntax && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
